# Зависимости офлайн-парсера (file_parser.py). Ставится одинаково на macOS и Windows:
#   pip install -r athlete_sweep/requirements-parser.txt
# Всё из бинарных колёс, компилятор не нужен.
# paramiko <4: sshtunnel 0.4 всё ещё зовёт paramiko.DSSKey, убранный в paramiko 5.
paramiko>=3,<4
sshtunnel>=0.4
psycopg[binary]>=3.1
beautifulsoup4>=4.12
# httpx тянется транзитивно: file_parser берёт store() из worker.py, а тот
# импортирует httpx на уровне модуля (нужен ему для фетча, парсеру — нет).
httpx>=0.27

